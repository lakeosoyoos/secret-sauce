# Generated outputs
report.html
report.pdf
*.html
*.pdf

# Sample/test data (don't publish customer traces)
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store
._*

# Local streamlit secrets
.streamlit/secrets.toml

# Error-report webhook URL — baked into the .exe at build time from the
# SLACK_ERROR_WEBHOOK CI secret. NEVER commit it (the repo is public; Slack
# auto-revokes webhooks found in public source).
_webhook.cfg
desktop/_webhook.cfg

# Temp
*.tmp

# desktop PyInstaller build artifacts
desktop/build/
desktop/dist/
desktop/build_env/
desktop/__pycache__/
desktop/*.zip
